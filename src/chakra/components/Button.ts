import { ComponentStyleConfig } from "@chakra-ui/react";

export const ButtonStyles: ComponentStyleConfig = { 
    variants: {
      primary: {
        bg: "brand.accent",
        color: "white",
      }
    }
  };